import React from 'react';
import DrawCarousel from './DrawCarousel';
import './Header.scss';
const Header = () => (
  <header className="draw-header relative">
    <DrawCarousel/>
  </header>
);

export default Header;
